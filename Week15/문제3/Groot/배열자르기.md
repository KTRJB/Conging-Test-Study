```swift
func 배열자르기(_ n:Int, _ left:Int64, _ right:Int64) -> [Int] {
    var result = [Int]()
    let n = Int64(n)

    for num in left...right {
        let i = num / n
        let j = num % n

        result.append(Int(max(i, j) + 1))
    }

    return result
}
```
